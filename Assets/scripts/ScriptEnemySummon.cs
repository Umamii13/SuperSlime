using TMPro;
using UnityEngine;

public class ScriptEnemySummon : MonoBehaviour
{
    /*public static ScriptEnemySummon Instance;
    public GameObject player;
    private UImanager killenemy;
    public GameObject Enemy;
    private EnemyHP enemydie;
    [Header("traget hero")]
    [SerializeField] private GameObject Enemy1;
    [SerializeField] private GameObject Enemy2;
    [SerializeField] private GameObject Enemy3;
    [SerializeField] private GameObject Enemy4;
    [SerializeField] private GameObject Enemy5;
    [SerializeField] private GameObject Enemy6;
    [SerializeField] private GameObject Enemy7;
    [SerializeField] private GameObject Enemy8;
    [SerializeField] private GameObject Enemy9;
    [SerializeField] private GameObject Enemy10;
    [SerializeField] private GameObject Enemy11;
    [SerializeField] private GameObject Enemy12;
    [SerializeField] private GameObject Enemy13;
    [SerializeField] private GameObject Enemy14;
    [SerializeField] private GameObject Enemy15;
    private void Awake()
    {
        killenemy = player.GetComponent<UImanager>();
        enemydie = Enemy.GetComponent<EnemyHP>();
    }
    public void EnemyDead()
    {
       
            if (Enemy1.active== false)
                killenemy.currenthero--;
            if (Enemy2.active== false)
                killenemy.currenthero--;
            if (Enemy3.active== false)
                killenemy.currenthero--;
            if (Enemy4.active== false)
                killenemy.currenthero--;
            if (Enemy5.active== false)
                killenemy.currenthero--;
            if (Enemy6.active== false)
                killenemy.currenthero--;
            if (Enemy7.active== false)
                killenemy.currenthero--;
            if (Enemy8.active== false)
                killenemy.currenthero--;
            if (Enemy9.active== false)
                killenemy.currenthero--;
            if (Enemy10.active== false)
                killenemy.currenthero--;
            if (Enemy11.active== false)
                killenemy.currenthero--;
            if (Enemy12.active== false)
                killenemy.currenthero--;
            if (Enemy13.active== false)
                killenemy.currenthero--;
            if (Enemy14.active== false)
                killenemy.currenthero--;
            if (Enemy15.active== false)
                killenemy.currenthero--;

        killenemy.SetEnemy();
    }*/

}
